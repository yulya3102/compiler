int fact(int n);

int main()
{
    int n;
    int res;
    while (true)
    {
        read(n);
        res = fact(n);
        write(res);
    }
    return 0;
}

int fact(int n)
{
    if (n < 2)
        return 1;
    else {}
    return n * fact(n - 1);
}
