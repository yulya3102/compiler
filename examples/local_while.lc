int main()
{
    int i; i = 0;
    while (i < 1000000)
    {
        write(i);
        i = i + 1;

        int a;  a = 5;
        if ((i + a) > 7)
        {
            int b;
            b = 42;
            write(b);
        }
        else
        {
            _Bool b;
            b = true;
            if (b)
                write(1);
            else
                write(0);
        }
    }

    return 0;
}
