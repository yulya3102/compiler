int fib(int n)
{
    if (n > 2)
        return fib(n - 1) + fib(n - 2);
    else
        return 1;
}

int main()
{
    int n;
    int res;
    while (read(n))
    {
        res = fib(n);
        write(res);
    }
    return 0;
}
