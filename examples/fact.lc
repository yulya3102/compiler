int fact(int n)
{
    if (n < 2)
        return 1;
    else {}
    return n * fact(n - 1);
}

int main()
{
    int n;
    int res;
    while (true)
    {
        read(n);
        res = fact(n);
        write(res);
    }
    return 0;
}
