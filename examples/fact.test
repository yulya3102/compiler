#!/bin/sh

set -e

bc > fact.expected << EOF
define fact(x)
{
    if (x < 2) return (1);
    return (x * fact(x - 1));
}

for (i = 1; i <= 10; i = i + 1)
    fact(i)
EOF

(seq 10 | `dirname "$0"`/fact | head) > fact.actual

diff -q fact.expected fact.actual
